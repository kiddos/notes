#include <bits/stdc++.h>

using namespace std;

using i64 = long long;

void solve() {
  string s;
  getline(cin, s);
  cout << "143" << endl;
}

int main(void) {
  ios::sync_with_stdio(false);
  cin.tie(0);

  solve();
  return 0;
}
