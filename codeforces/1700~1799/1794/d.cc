#include <bits/stdc++.h>

using namespace std;

using i64 = long long;

// 2 2 3 5
//
// (2^3 * 3^5), (2^5 * 3^2), (2^2 * 5^3), (2^3 * 5^2), (3^2 * 5^2)
// so select n/2 unique primes and count how many unique number is left

void solve() {
  int n = 0;
  cin >> n;


}

int main(void) {
  ios::sync_with_stdio(false);
  cin.tie(0);

  solve();
  return 0;
}
